[package]
name = "vera-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Vera domain types and the agent-facing output contract. Zero I/O."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
