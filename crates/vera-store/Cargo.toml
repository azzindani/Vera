[package]
name = "vera-store"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Corpus access behind one read-only trait. SQLite today, Postgres/pgvector next."

[dependencies]
vera-core = { version = "0.0.1", path = "../vera-core" }
rusqlite = { version = "0.37", features = ["bundled"] }
serde_json = "1"
thiserror = "2"

[lints]
workspace = true

[dev-dependencies]
tempfile = "3"
